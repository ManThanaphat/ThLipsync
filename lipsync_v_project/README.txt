# TH LipSync Studio

✨ โปรแกรมสร้างการขยับปากอัตโนมัติจากเสียงพูด (ภาษาไทย)

---

## วิธีติดตั้งและใช้งาน

1. ดาวน์โหลดโฟลเดอร์โปรเจกต์ทั้งหมด (หรือไฟล์ ZIP)
   ตัวอย่างโครงสร้างไฟล์:

   ```
   lipsync_v_project/
   ├─ models/
   ├─ mouth_sets/
   ├─ program.exe
   ├─ README.txt
   ```

2. แตกไฟล์ ZIP ไปยังที่ที่ต้องการ เช่น Desktop หรือ D:\Projects\

3. ดับเบิลคลิก `program.exe` เพื่อเปิดโปรแกรม
   (ไม่ต้องติดตั้ง Python หรือโปรแกรมเสริมอื่น ๆ)

4. ภายในโปรแกรม:

   * กด **📂 เลือกไฟล์เสียง (.wav)** เพื่อโหลดเสียงพูดภาษาไทย
   * โปรแกรมจะวิเคราะห์เสียงและแสดงภาพขยับปากอัตโนมัติ
   * ปรับ **Noise Gate (dB)** เพื่อแยกเสียงได้แม่นยำยิ่งขึ้น
   * กด **🎬 ส่งออกวิดีโอ** เพื่อบันทึกไฟล์ .mp4 พร้อมเสียง

---

## คำแนะนำเพิ่มเติม

* ควรใช้ไฟล์เสียงที่เป็น **.wav 16kHz mono** เพื่อความแม่นยำ
* ต้องมีโฟลเดอร์ `models` และ `mouth_sets` อยู่ในที่เดียวกับ `program.exe`
* ถ้าโปรแกรมแจ้งว่า "No models loaded"
  ให้ตรวจสอบว่าใน `models/` มีไฟล์ `hmm_A.pkl` ถึง `hmm_U.pkl` อยู่ครบ

---

## ติดต่อผู้พัฒนา

ผู้พัฒนา: ธนภัทร นาคอ่อน
โครงการ: TH LipSync Studio
สร้างด้วย Python + Tkinter + hmmlearn + librosa + ffmpeg

⁠CS project License